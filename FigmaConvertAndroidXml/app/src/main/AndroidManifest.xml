<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="mrusta.FigmaConvertAndroidXml">

    <!-- Ключи доступа разрешения -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Доступ локации -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/Test">

        <activity
            android:name=".ui.LaunchActivity">
            <intent-filter>
                <action   android:name="android.intent.action.MAIN"       />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_figma_convert_xml"
            android:theme="@style/MyTheme" />

        <activity
            android:name=".framework.recyclerAdapter.Interface.RecyclerAdapterActivity"
            android:label="RecyclerAdapter Activity"
            android:theme="@style/MyTheme" />

    </application>

</manifest>